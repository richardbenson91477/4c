#
# large comments look like this
#

# main receives no arguments and returns a number
main% @do
    # $print^ receives a string and returns a boolean
    $print^ ("Hello, world!" $+ 'nl $+ 'nil)
    # return receives whatever type the enclosing @do expects
    @return 0
@done

