#
(def main (do
    (set i 4)
    (while (> i 0)
        (print$ (str-from ('char i) 'spc 'nil))

        (set j 4)
        (while (> j 0)
            (print$ (str-from ('char (+ j 64)) 'nil))
          (set j (- j 1))
        )

        (print$ (str-from 'nl 'nil))
      (set i (- i 1))
    )

    (print$ (str-from 'nl 'nil))
  0)
)

