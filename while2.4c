#
(main (do
    (i 4)
    (while (> i 0)
        (print$ ($+' ('char i) 'spc 'nil)

        (j 4)
        (while (> j 0)
            (print$ ($+' ('char (+ j 64)) 'nil)
          (j (- j 1))
        )

        (print$ ($+' 'nl 'nil))
      (i (- i 1))
    )

    (print$ ($+' 'nl 'nil))
) 0)

